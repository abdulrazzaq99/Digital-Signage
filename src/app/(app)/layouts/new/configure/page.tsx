import { ConfigureTemplate } from "@/components/layouts/configure-template";
import { Suspense } from "react";

export default function Page() {
  return <Suspense><ConfigureTemplate /></Suspense>;
}
